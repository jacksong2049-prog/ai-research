name: Update Time Leaderboard
on:
  push:
    branches: [master]
  workflow_dispatch:
permissions:
  issues: write
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - run: echo test
